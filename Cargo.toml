[package]
name = "theos"
version = "0.1.0"
edition = "2021"

[lib]
name = "kernel"
path = "src/kernel/lib.rs"
crate-type = ["staticlib"]
test = false
bench = false

[profile.dev]
panic = "abort"

[profile.release]
panic = "abort"