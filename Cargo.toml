[package]
name = "theos"
version = "0.1.0"
edition = "2021"

[lib]
name = "kernel"
path = "src/kernel/lib.rs"  # Pointing directly to your library file
crate-type = ["staticlib"]

[profile.dev]
panic = "abort"

[profile.release]
panic = "abort"